<h1>Cart#index</h1>

<% if user_signed_in? %>
<div>
<table class="table">
  <thead>
	  <tr>
      <td>Minitature</td>
      <td>Nom de l'affiche</td>
	  	<td>Description</td>
			<td></td>
		</tr>
	</thead>
  <tbody id = "<%=@cart.id%>">
  <%if @content%>
  <%= render 'layouts/content' %>
  <% end %>
  </tbody>
</table>
<p><%=@cart.id%></p>
<% else%>
<p>Enregistrez vous pour avoir un panier</p>
<% end %>