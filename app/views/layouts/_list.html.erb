<ul class = "Commandes">
<% @orders.each do |order| %>
    <li class='email'>
    <%= link_to "Commande #{order.id}", "/show_orders/#{order.id}", class: 'show list-group-item list-group-item-action', id: order.id, remote: true %>
    </li>
<% end %>
</ul>